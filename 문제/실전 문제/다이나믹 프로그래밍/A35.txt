이 코드는 "못생긴 수"를 찾는 코드이다.
못생긴수는 그 수의 소인수가 2, 3, 5 만으로 이루어진 수를 의미한다. 

못생긴 수를 저장한 리스트 uglyNum을 생성, 이는 DP를 사용함을 의미한다.

여기서 생각해 볼만한 점은 "uglyNum[i] * (2 or 3 or 5)" 는 무조건 못생긴 수라는 점이다.
따라서 2, 3, 5를 곱한 수에서 제일 작은 수가 다음 uglyNum에 들어가게 된다.
이를 for 문으로 n번 반복하면 n번째 못생긴수를 찾을수 있다.